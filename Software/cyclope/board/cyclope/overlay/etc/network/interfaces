auto lo
iface lo inet loopback

auto wlan0
iface wlan0 inet dhcp
  pre-up wpa_supplicant -B -Dnl80211 -iwlan0 -c/media/data/etc/wpa_supplicant/wpa_supplicant.conf -s
  post-down killall -q wpa_supplicant
  wait-delay 15
